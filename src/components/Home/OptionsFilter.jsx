

const OptionsFilter = ({ name }) => {
  return (
    <select name={name} className="bg-transparent focus:bg-black">
      <option>example 1</option>
      <option>example 2</option>
      <option>example 3</option>
    </select>
  );
}

export default OptionsFilter;
